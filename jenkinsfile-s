node('workstation') {
  try{
    stage ("one"){
       echo "one"
       exit 1
          }


    stage ("two"){
     echo "two"
     }
  } catch(e) {
         stage('Declarative : post actions') {
            echo 'this will be run if any failed'
              }
  }
}